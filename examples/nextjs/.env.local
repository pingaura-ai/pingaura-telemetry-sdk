# Point the SDK at the in-app mock collector (app/api/collect) so we can inspect
# what gets emitted. In a real app you would leave PINGAURA_ENDPOINT unset.
PINGAURA_INGEST_KEY=pa_test_key
PINGAURA_TELEMETRY_DOMAIN=example.com
PINGAURA_ENDPOINT=http://localhost:3210/api/collect
