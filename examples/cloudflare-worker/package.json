{
  "name": "pingaura-telemetry-example-cloudflare-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@pingaura/telemetry": "^0.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
